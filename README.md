# security-validate_http_options.ps1
PowerShell tool to validate whether the HTTP OPTIONS method is enabled across a list of servers. Supports HTTPS, optional HTTP fallback, TLS validation bypass for internal testing, and produces a timestamped CSV-style log suitable for PCI remediation evidence.
